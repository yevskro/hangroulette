class ServerSession{
    constructor(session){
        this.players = () => {}
        
        this.addPlayer = (client) => {

        }

        this.removePlayer = (client) => {
        
        }

        this.playerGuess = (guess) => {
        
        }

        const plyrClients = []
        let session = session
    }
}